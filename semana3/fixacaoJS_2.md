```function calculaPrecoTotal(quantidade) { ```
 ``` // Escreva seu código aqui ```
  ``` let calculoTotal = precoMaca * quantidade ```
  
  ```if(quantidade <= 11) {```
  ``` precoMaca = 1.30```
  ```} else {```
  ``` precoMaca = 1```
 ``` }```
  ```let calculoTotal = precoMaca * quantidade```
 ``` return calculoTotal```
```}`